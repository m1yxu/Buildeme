
  # Create global CSS file

  This is a code bundle for Create global CSS file. The original project is available at https://www.figma.com/design/YI0vgf5Jbi7QSEPCOJQDpm/Create-global-CSS-file.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  